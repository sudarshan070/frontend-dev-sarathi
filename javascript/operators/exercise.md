 ```js
 console.log(true || true) 

 console.log(true && true) 

 console.log(false && false) 

 console.log(0 || false || "Rahul" || "Aniket")
  
 console.log(0 && false && "Rahul" && "Aniket") 
 
 let firstName = ""
 let lastName = ""
 let nickName = ""
 console.log(firstName || lastName|| nickName || "nothing")
 
 
 let name = "Ravan"
 let work = "War"
 
 if(name == "Ravan" && work == "War"){
  console.log(`I'm ${name}, my work ${work}`)
 } else {
  console.log('nothing')
 }
 
 
let check = (name == "Ravan" || work == "klwfeke") ? "Ravan" :"No";
console.log(check)

alert( 1 && null && 2 );

alert( alert(1) || 2 || alert(3) );

alert( 1 && null && 2 );

alert( null || 2 && 3 || 4 );

alert( Boolean("non-empty string") );

alert( Boolean(null) ); 

```
